<div align="center">

# Отчет

</div>

<div align="center">

## Практическая работа №12

</div>

<div align="center">

## Типы активностей. Шаблоны Android Studio. Сохранение настроек с SharedPreferences

</div>

**Выполнил:**  
Покидов Матвей Юрьевич

**Курс:** 2

**Группа:** ИНС-б-о-24-1

**Направление:** 09.03.02

**Профиль:** Информационные системы и технологии

---

### Цель работы

Изучить различные типы шаблонов активностей, предоставляемых Android Studio. Научиться создавать многоэкранные приложения с использованием разных видов окон. Освоить механизм сохранения простых пользовательских настроек с помощью `SharedPreferences`.

### Задания для самостоятельного выполнения

**Вариант 11: Волк ловит яйца**

Создать игровое приложение «Волк ловит яйца» со следующими экранами:
- **Главный экран** — навигация по приложению, отображение текущих настроек.
- **Экран игры** — имитация игрового процесса с подсчетом очков.
- **Экран настроек** — сохранение параметров игры с использованием `SharedPreferences`.
- **Экран рекордов** — отображение лучшего результата и статистики игр.

---

### Ход работы

#### 1. Создание проекта и настройка манифеста

Был создан новый проект `WolfCatchEggs` с использованием языка Kotlin и шаблона **Empty Views Activity**. В файл манифеста (`AndroidManifest.xml`) добавлены необходимые разрешения: `INTERNET` для возможной загрузки ресурсов и `VIBRATE` для тактильной обратной связи в игре. Все активности зарегистрированы с указанием родительской активности для корректной навигации.

<div align="center">

![Файл AndroidManifest.xml](Рисунок9.png)

**Рисунок 1** — Файл AndroidManifest.xml с разрешениями и зарегистрированными активностями

</div>

#### 2. Создание главного экрана (MainActivity)

Главный экран создан на основе шаблона **Empty Activity** и доработан.

При запуске и возврате на главный экран (`onResume`) происходит чтение настроек из `SharedPreferences` и их отображение в текстовом поле.

<div align="center">

![Главный экран приложения](Рисунок10.png)

**Рисунок 2** — Главный экран приложения «Волк ловит яйца»

</div>

#### 3. Создание экрана настроек (SettingsActivity)

Экран настроек создан с использованием шаблона **Settings Activity**. Настройки описаны в XML-файле `root_preferences.xml` и включают:
- `EditTextPreference` — ввод имени игрока.
- `ListPreference` — выбор сложности (Легкая, Средняя, Сложная).
- `SwitchPreference` — включение/выключение звука.
- `SwitchPreference` — включение/выключение вибрации.

Все значения автоматически сохраняются в `SharedPreferences`.

<div align="center">

![Экран настроек](Рисунок11.png)

**Рисунок 3** — Экран настроек с параметрами игры

</div>

#### 4. Создание экрана игры (GameActivity)

Экран игры создан на основе шаблона **Empty Activity**. Реализована имитация игрового процесса:
- Счёт отображается в верхней части экрана.
- Каждые 2 секунды «падает» яйцо (имитация через `Handler`).
- При нажатии на кнопку «Поймать яйцо» с вероятностью 50% игрок ловит или пропускает яйцо.
- Очки зависят от выбранной сложности.
- При успешной поимке или промахе срабатывает вибрация (если включена в настройках).

<div align="center">

![Экран игры - падение яйца](Рисунок12.png)

**Рисунок 4** — Игровой процесс: яйцо падает

</div>

<div align="center">

![Экран игры - промах](Рисунок13.png)

**Рисунок 5** — Игровой процесс: промах

</div>

#### 5. Создание экрана рекордов (RecordsActivity)

Экран рекордов отображает:
- Лучший счёт и имя рекордсмена.
- Статистику: количество сыгранных игр и общее количество пойманных яиц.
- Кнопку «Сбросить рекорды» для очистки статистики.
- Кнопку «Назад» для возврата на главный экран.

Данные загружаются из `SharedPreferences` при открытии экрана.

<div align="center">

![Экран рекордов](Рисунок14.png)

**Рисунок 6** — Экран рекордов с отображением лучшего результата и статистики

</div>

#### 6. Навигация между экранами

Переходы между активностями реализованы с использованием класса `Intent`.

---

### Вывод

В ходе выполнения практической работы №12 (самостоятельное задание, вариант 11):

1. **Изучены шаблоны активностей Android Studio:**
   - **Empty Activity** — использован для главного экрана, экрана игры и экрана рекордов.
   - **Settings Activity** — использован для создания экрана настроек с автоматическим сохранением параметров.

2. **Освоен механизм SharedPreferences:**
   - Настройки сохраняются в общем хранилище приложения.
   - Реализовано чтение и запись данных различных типов (`String`, `Boolean`).
   - Настройки отображаются на главном экране и влияют на игровой процесс (сложность, вибрация).

3. **Создано многоэкранное приложение:**
   - Реализовано 4 активности с корректной навигацией.
   - Настроены переходы через `Intent`.
   - Обеспечена обратная навигация через Action Bar.

4. **Реализована система рекордов:**
   - Лучший результат сохраняется в `SharedPreferences`.
   - Ведется статистика сыгранных игр и пойманных яиц.

5. **Использованы современные практики разработки:**
   - Все строки вынесены в ресурсы (`strings.xml`).
   - Использованы KTX-расширения для работы с `SharedPreferences`.

---

### Ответы на контрольные вопросы

**1. Какие шаблоны активностей предоставляет Android Studio? Кратко опишите назначение 3-4 из них.**

- **Empty Activity** — пустая активность с минимальной разметкой. Универсальный шаблон для любых экранов.
- **Basic Activity** — включает `AppBarLayout`, `Toolbar` и `FloatingActionButton`. Подходит для приложений с верхней панелью и плавающей кнопкой.
- **Settings Activity** — генерирует экран настроек на основе `PreferenceFragmentCompat`. Автоматически сохраняет значения в `SharedPreferences`.
- **Navigation Drawer Activity** — создает активность с боковым выдвижным меню (Navigation Drawer).

**2. Для чего используется SharedPreferences? Какие типы данных можно в нём хранить?**

`SharedPreferences` используется для хранения небольших объемов данных в виде пар «ключ-значение». Поддерживаемые типы: `String`, `Int`, `Long`, `Float`, `Boolean`, `Set<String>`.

**3. В чём разница между методами getPreferences(), getSharedPreferences() и PreferenceManager.getDefaultSharedPreferences()?**

- `getPreferences(MODE)` — возвращает настройки, доступные только в рамках текущей активности (имя файла = имя класса).
- `getSharedPreferences(NAME, MODE)` — возвращает настройки с указанным именем, доступные из любой активности.
- `PreferenceManager.getDefaultSharedPreferences(Context)` — возвращает настройки по умолчанию для всего приложения (используется с `Settings Activity`).

**4. Как записать данные в SharedPreferences? Объясните разницу между apply() и commit().**

```kotlin
prefs.edit().putString("key", "value").apply()
```
- `apply()` — асинхронная запись, не возвращает результат, рекомендуется для UI-потока.
- `commit()` — синхронная запись, возвращает `Boolean` (успех/неудача), может блокировать поток.

**5. Как прочитать данные из SharedPreferences? Для чего нужно значение по умолчанию?**

```kotlin
val value = prefs.getString("key", "значение по умолчанию")
```
Значение по умолчанию возвращается, если ключ не найден в хранилище. Это предотвращает ошибки `null` и обеспечивает корректную работу при первом запуске.

**6. Как создать экран настроек с использованием шаблона Settings Activity? Где описываются элементы настроек?**

При создании активности выбирается шаблон **Settings Activity**. Элементы настроек описываются в XML-файле, который находится в папке `res/xml/` (например, `root_preferences.xml`). Для каждого элемента указывается ключ (`key`), заголовок, значение по умолчанию и другие атрибуты.

**7. Как организовать переход между активностями с помощью Intent?**

```kotlin
val intent = Intent(this, TargetActivity::class.java)
startActivity(intent)
```
Для возврата на предыдущий экран используется `finish()` или кнопка «Назад» в Action Bar.

**8. Что такое FloatingActionButton и в каких шаблонах он присутствует?**

`FloatingActionButton` (FAB) — круглая кнопка, расположенная над содержимым экрана, обычно в правом нижнем углу. Используется для основного действия на экране. Присутствует в шаблонах **Basic Activity** и **Scrolling Activity**.
